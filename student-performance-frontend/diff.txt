diff --git a/student-performance-frontend/src/App.jsx b/student-performance-frontend/src/App.jsx
index 0dd8b5f..939ab46 100644
--- a/student-performance-frontend/src/App.jsx
+++ b/student-performance-frontend/src/App.jsx
@@ -17,7 +17,7 @@ import StudentsDirectoryPage from './components/pages/StudentsDirectoryPage'
 import Chatbot from './components/Chatbot'
 import LoadingScreen from './components/LoadingScreen'
 import { Sun, Moon, Bell, User, LogOut, X, Menu } from 'lucide-react'
-import { Toaster } from 'react-hot-toast'
+import toast, { Toaster } from 'react-hot-toast'
 
 function App() {
   const [loading, setLoading] = useState(false)
@@ -26,7 +26,6 @@ function App() {
     const saved = localStorage.getItem('predictionHistory');
     return saved ? JSON.parse(saved) : [];
   });
-  const [error, setError] = useState(null)
 
   useEffect(() => {
     localStorage.setItem('predictionHistory', JSON.stringify(history));
@@ -82,16 +81,25 @@ function App() {
 
   const handlePredict = async (formData) => {
     setLoading(true)
-    setError(null)
     setResult(null)
 
     try {
-      const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:5000';
-      const response = await fetch(`${API_URL}/predict`, {
-        method: 'POST',
-        headers: { 'Content-Type': 'application/json' },
-        body: JSON.stringify(formData)
-      })
+      let response;
+      try {
+        // fast local path
+        response = await fetch(`http://localhost:5000/predict`, {
+          method: 'POST',
+          headers: { 'Content-Type': 'application/json' },
+          body: JSON.stringify(formData)
+        })
+      } catch (localErr) {
+        // fallback to production API if local server is not running
+        response = await fetch(`https://student-performance-ve59.onrender.com/predict`, {
+          method: 'POST',
+          headers: { 'Content-Type': 'application/json' },
+          body: JSON.stringify(formData)
+        })
+      }
 
       if (!response.ok) throw new Error('Failed to fetch prediction from server')
 
@@ -108,8 +116,9 @@ function App() {
       
       setResult(newResult)
       setHistory(prev => [newResult, ...prev].slice(0, 20))
+      toast.success('Prediction generated successfully!')
     } catch (err) {
-      setError(err.message || 'An unexpected error occurred')
+      toast.error(err.message || 'An unexpected error occurred')
     } finally {
       setLoading(false)
     }
@@ -156,20 +165,10 @@ function App() {
               </div>
               {history.length > 0 && (
                 <div className="results-column">
-                  {error && (
-                    <div className="error-message">
-                      <p>{error}</p>
-                    </div>
-                  )}
                   <PredictionHistory history={history} />
                 </div>
               )}
             </div>
-            {error && !history.length && (
-              <div className="error-message max-w-2xl mx-auto mt-4">
-                <p>{error}</p>
-              </div>
-            )}
             <Footer />
           </>
         );
